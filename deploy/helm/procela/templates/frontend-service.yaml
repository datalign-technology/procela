apiVersion: v1
kind: Service
metadata:
  name: {{ include "procela.fullname" . }}-frontend
  labels:
    {{- include "procela.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.frontend.containerPort }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "procela.selectorLabels" (dict "root" . "component" "frontend") | nindent 4 }}
